rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-store POS: All store devices & partners share access to all collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
